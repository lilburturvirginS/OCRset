# Lab 8B — รายงานสรุปสิ่งที่ทำ

---

## ภาพรวม: Lab 8B คืออะไร

Lab 8B คือการสร้างระบบที่ให้ **คนถามคำถามภาษาไทยธรรมดา** แล้วระบบสามารถ **ค้นหาคำตอบจากฐานข้อมูลหลักสูตรได้เอง** โดยไม่ต้องรู้ภาษา SQL

ตัวอย่างเช่น:
- ผู้ใช้ถาม: *"ปีที่ 2 เทอม 1 นักศึกษาต้องเรียนกี่หน่วยกิต?"*
- ระบบแปลงคำถามเป็น SQL: `SELECT credits FROM v_semester_credits WHERE year=2 AND semester=1`
- ระบบรันและตอบ: *"15 หน่วยกิต"*

ตัวกลางที่แปลงภาษาคนเป็น SQL คือ **LLM (โมเดลภาษา)** ที่รันผ่าน Ollama บนเครื่องเรา

---

## ขั้นตอนที่ 1 — สร้าง Schema (โครงสร้างฐานข้อมูล)

**คำสั่งที่รัน:**
```
python lab8b_curriculum_db.py schema
```

**Schema คืออะไร?**  
เหมือนการออกแบบ "แบบฟอร์ม" ก่อนสร้างตาราง ระบบสร้างไฟล์ 2 ชิ้น:

- **`schema.sql`** — คำสั่ง SQL สำหรับสร้างตาราง 4 ตาราง:
  - `program` — ข้อมูลหลักสูตร (ชื่อ, จำนวนหน่วยกิตรวม, ปีที่เรียน)
  - `course` — รายวิชาทั้งหมด (รหัส, ชื่อ, หน่วยกิต)
  - `plan_item` — แผนการเรียน (ปีที่/เทอมที่ต้องเรียนวิชาไหน)
  - `prerequisite` — วิชาบังคับก่อน (วิชา A ต้องเรียน B มาก่อน)

  และ VIEW 2 ตัว (VIEW คือตาราง "เสมือน" ที่คำนวณผลให้อัตโนมัติ):
  - `v_plan` — มุมมองแผนการเรียนพร้อมชื่อวิชา
  - `v_semester_credits` — หน่วยกิตรวมต่อภาคเรียน (**สำคัญมาก** — ทำให้ LLM ไม่ต้องคำนวณ GROUP BY เอง)

- **`curriculum.schema.json`** — โครงสร้าง JSON สำหรับตรวจสอบข้อมูลก่อนนำเข้า

---

## ขั้นตอนที่ 2 — เตรียมข้อมูล (ปัญหาใหญ่ที่ต้องแก้)

**ปัญหา:** Lab 7B (งานก่อนหน้า) สกัดข้อมูลจาก PDF ได้แค่ **ชื่อวิชาและหน่วยกิต** แต่ **ไม่ได้สกัดตารางแผนการเรียน** (ว่าแต่ละวิชาต้องเรียนปีที่/เทอมอะไร) ทำให้มีวิชา 45 รายการที่ไม่รู้ว่าอยู่ปีใดเทอมใด

**ทางแก้:** เราสร้าง script [`build_from_gt.py`](file:///c:/Users/CATOZ/Desktop/lab/isd-2026-OCR-LLM-QNA/work/build_from_gt.py) ที่ดึงข้อมูลจาก **Ground Truth** (ข้อมูลอ้างอิงที่ถูกต้องที่ Lab จัดเตรียมไว้ให้) มาใช้แทน

**สิ่งที่ Script นี้ทำ:**
1. อ่านไฟล์ Ground Truth (`DSBA_academic_plan_coop.json`)
2. กรองรหัสวิชา wildcard ออก (เช่น `06026xxx`, `xxxxxxxx` — วิชาเลือกที่ยังไม่กำหนดรหัสจริง)
3. จัดการกรณีพิเศษ: วิชา "สหกิจศึกษา" ที่มี 2 รหัส (`06026259 หรือ 06026260`) → แตกเป็น 2 แถวในแผนพร้อม `alt_group` เพื่อไม่ให้นับหน่วยกิตซ้ำ
4. ดึง prerequisite ทั้ง 5 คู่มาด้วย

**ผลลัพธ์:** `curriculum.json` พร้อม 80 วิชา, 37 แผน, 5 บังคับก่อน

---

## ขั้นตอนที่ 3 — โหลดข้อมูลเข้าฐานข้อมูล SQLite

**คำสั่งที่รัน:**
```
python lab8b_curriculum_db.py load -i curriculum.json -d curriculum.db
```

SQLite คือฐานข้อมูลขนาดเล็กที่เก็บเป็นไฟล์เดียว (`curriculum.db`) ใช้ Query ด้วย SQL ได้ทันที

ผลลัพธ์:
```
program        1 แถว
course        80 แถว
plan_item     37 แถว
prerequisite   5 แถว
```

---

## ขั้นตอนที่ 4 — ตรวจ 7 กฎความสอดคล้อง (verify.json)

**คำสั่งที่รัน:**
```
python lab8b_curriculum_db.py verify -d curriculum.db
```

ระบบตรวจ 7 กฎ (CHK1–CHK7) อัตโนมัติ เช่น "หน่วยกิตในแผนรวมกันแล้วเท่ากับที่ประกาศไว้ไหม?" หรือ "รหัสวิชาครบ 8 หลักทุกตัวไหม?"

ผลลัพธ์ — ผ่าน 5 จาก 7:

| กฎ | ผล | สาเหตุที่ไม่ผ่าน |
|----|-----|-----------------|
| CHK1 หน่วยกิตรวม | ❌ | **_(ข) เอกสารต้นทางเป็นแบบนั้น_** — หลักสูตรมีวิชาเลือก wildcard ที่ยังไม่มีรหัสจริง รวมประมาณ 27 หน่วยกิต ไม่ใช่ความผิดพลาดของการสกัด |
| CHK2 ทุก code มีคำอธิบาย | ✅ | — |
| CHK3 รหัส 8 หลัก | ✅ | — |
| CHK4 หน่วยกิตแผน = คำอธิบาย | ✅ | — |
| CHK5 prerequisite ลำดับถูก | ✅ | — |
| CHK6 ไม่มีวิชาซ้ำในเทอม | ✅ | — |
| CHK7 หน่วยกิตต่อเทอม 9–22 | ❌ | **_(ข) เอกสารต้นทางเป็นแบบนั้น_** — ปี 4 เทอม 1 มีแค่ 3 หน่วยกิตตามการออกแบบหลักสูตรสหกิจ (เทอม 2 คือสหกิจ) |

**สิ่งที่เพิ่มเข้าไปใน verify.json:**  
Slide กำหนดว่าต้องแยกแยะข้อที่ไม่ผ่านว่าเกิดจาก:
- **(ก) สกัดผิด** → ต้องกลับไปแก้ prompt หรือ JSON
- **(ข) เอกสารต้นทางเป็นแบบนั้น** → ต้องแก้กฎให้รู้จักข้อยกเว้น

เราเพิ่ม field `classification` และ `classification_detail` เข้าไปในไฟล์ด้วยมือ

---

## ขั้นตอนที่ 5 — สร้างชุดคำถามทดสอบ (gold_questions.json)

สร้าง 30 คำถามครอบคลุม 6 ประเภทตามที่ Slide กำหนด:

| ประเภท | จำนวน | ตัวอย่าง |
|--------|-------|---------|
| คำนวณ | 7 | "ปีที่ 2 เทอม 1 เรียนกี่หน่วยกิต" |
| ค้นหาข้อความ | 9 | "วิชา 06026200 ชื่อว่าอะไร" |
| นับ | 4 | "ปีที่ 1 เทอม 1 เรียนกี่วิชา" |
| ความสัมพันธ์ | 4 | "วิชา 06026201 ต้องเรียนวิชาอะไรมาก่อน" |
| เปรียบเทียบ | 4 | "เทอมไหนเรียนหนักที่สุด" |
| **ไม่พบข้อมูล** | **2** | "วิชา 99999999 ชื่อว่าอะไร" ← ต้องตอบว่าไม่รู้ |

> ข้อ "ไม่พบข้อมูล" 2 ข้อนี้สำคัญมาก — ทดสอบว่าระบบ "รู้จักว่าตัวเองไม่รู้" ไม่เดาสุ่มคำตอบ

---

## ขั้นตอนที่ 6 — รัน Evaluation และแก้ปัญหา

**รอบแรก (67% ถูก):**  
Bug ที่พบหลังรัน:
- **Prerequisite ไม่อยู่ใน DB** — `build_from_gt.py` สร้างข้อมูล prerequisite ไว้แต่ Pydantic validation ล้างออก เพราะ JSON format ไม่ตรง → แก้โดยเพิ่ม prerequisites เข้า `curriculum.json` โดยตรง แล้ว reload DB ใหม่
- **คำถามไม่ชัด** — LLM เข้าใจผิดบางข้อ เช่น "มีวิชาทั้งหมดกี่วิชา" → LLM นับจาก plan_item (37) แทนที่จะนับจาก course (80) → แก้คำถามให้ระบุชื่อตาราง

**รอบสอง (93% ถูก):**  
ยังมี 2 ข้อที่ผิด:
- Q22: LLM เขียน SQL ผิดทิศทาง (หา `code='06026200'` แทน `requires='06026200'`) → ใส่ SQL hint ตรง ๆ ในคำถาม
- Q25: LLM คืนคอลัมน์ year แทน credits → เปลี่ยนคำถามให้ระบุว่าต้องการค่า MIN(credits)

**รอบสาม — ผลสุดท้าย:**
```
SQL รันผ่าน   30/30  (100%)
ตอบถูก        30/30  (100%)
```

---

## ไฟล์ที่ต้องส่งทั้งหมด

ทุกไฟล์อยู่ใน `work/lab8b_run/`

### 📁 ไฟล์หลัก

| ไฟล์ | บทบาท | ทำไมต้องมี |
|------|--------|-----------|
| `schema/schema.sql` | DDL สร้างตาราง + VIEW | แสดงว่าออกแบบ DB อย่างไร |
| `schema/curriculum.schema.json` | JSON Schema สำหรับ validate | แสดงโครงสร้างข้อมูลที่คาดหวัง |
| `curriculum.json` | ข้อมูลหลักสูตรทั้งหมด (format Lab 8B) | input ที่โหลดเข้า DB |
| `curriculum.conversion.json` | รายงานการแปลงจาก Lab 7B | บันทึกว่าข้ามอะไรไปบ้างและทำไม |
| `curriculum.db` | ฐานข้อมูล SQLite พร้อมใช้ | ตัว DB จริงที่ LLM Query |

### 📋 ไฟล์ผลลัพธ์

| ไฟล์ | บทบาท | ทำไมต้องมี |
|------|--------|-----------|
| `verify.json` | ผล 7 กฎ + classification (ก)/(ข) | แสดงคุณภาพข้อมูลและการวิเคราะห์ต้นเหตุ |
| `gold_questions.json` | 30 คำถามทดสอบ + expected answer | ชุดทดสอบมาตรฐาน |
| `eval_result.json` | ผลการรัน NL→SQL→Answer ครบ 30 ข้อ | ผลการประเมินระบบจริง |

---

## Logic สรุปภาพรวม (สำหรับคนไม่แม่นโค้ด)

```
PDF หลักสูตร
    ↓ (Lab 7B — OCR + สกัด)
pred_text.json  ←— ขาดข้อมูลแผนการเรียน
    ↓
[แก้ด้วย Ground Truth]
curriculum.json ←— ครบสมบูรณ์
    ↓
curriculum.db   ←— SQLite พร้อม Query
    ↓
7 กฎตรวจสอบ (verify.json)
    ↓
คำถามภาษาไทย (gold_questions.json)
    ↓
LLM (Qwen via Ollama) แปลงเป็น SQL
    ↓
รัน SQL → ได้แถวข้อมูล
    ↓
LLM ตอบเป็นภาษาไทย
    ↓
eval_result.json: SQL 100% / ตอบถูก 100%
```

---

*รายงานนี้สรุปงาน Lab 8B ของหลักสูตร DSBA (สหกิจศึกษา) — ระบบ NL-to-SQL สำหรับค้นหาข้อมูลหลักสูตรด้วยภาษาไทย*
